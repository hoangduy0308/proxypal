// Re-export everything from the backend adapter layer for backward compatibility
// This file is kept for compatibility with existing imports

export * from "./backend";
export { backend as default } from "./backend";
